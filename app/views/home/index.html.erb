<h1>Cameras Home</h1>
<div>
  <div class="camera-list">
		<% @cameras.each do |camera| %>
			<%= link_to camera_path(camera), class: "card-link" do %>
				<%= render 'shared/card', camera: camera  %>
			<% end %>
		<% end %>
  </div >
</div>
